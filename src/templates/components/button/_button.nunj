{% macro button(props, attr = '', tag = 'button') %}
<{{ tag }} class="button _{{ props.type }}{% if props.className %} {{ props.className }}{% endif %}" {{ attr }}>
	{{ props.text }}
</{{ tag }}>
{% endmacro %}
