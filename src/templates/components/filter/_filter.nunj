<section class="filter">
    <ul class="filter-tabs-nav tabs-nav _by-click">
        {% for nav in categories %}
            <li class="filter-tabs-nav__item tabs-nav__item" data-tab-opener="{{ nav.slug }}" data-auto-show>{{ nav.name }}</li>
        {% endfor %}
    </ul>
</section>